<%
  breadcrumb :payments
  @title = 'Payment Transactions'
%>

<p>Here is a list of all of the payment transactions on your account.</p>

<% if @payments.any? %>
  <div class="table-responsive">
    <table class="table table-striped table-condensed">
      <thead>
        <tr>
          <th>Status</th>
          <th>Amount</th>
          <th>Date</th>
          <th>Card Holder</th>
          <th>Card Number</th>
        </tr>
      </thead>
      <tbody>
        <% @payments.each do |payment| %>
          <tr>
            <th><%= payment.status.humanize %></th>
            <th><%= number_to_currency payment.amount_pounds, unit: '£' %></th>
            <th><%= format_date payment.created_at %></th>
            <th><%= payment.card_holder %></th>
            <th>************<%= payment.last_four_digits %></th>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
<% else %>
  <p class="text-darkish"><em>There are no transactions to display</em></p>
<% end %>