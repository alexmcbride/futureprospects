<%
  breadcrumb :staff_applications
  @title = 'Applications'
%>

<div class="table-responsive">
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Student</th>
        <th>Submitted</th>
        <th>Updated</th>
        <th>Status</th>
        <th colspan="2"></th>
      </tr>
    </thead>

    <tbody>
      <% @applications.each do |application| %>
        <tr>
          <td><%= application.student.full_name %></td>
          <td title="<%= format_datetime application.submitted_date %>"><%= format_date application.submitted_date %></td>
          <td title="<%= format_datetime application.updated_at %>"><%= format_date application.updated_at %></td>
          <td><%= application.status.humanize %></td>
          <td><%= link_to 'View', staff_application_path(application) %></td>
          <td><%= link_to 'Edit', edit_staff_application_path(application) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

