<% breadcrumb :staff_edit_course, @staff_course %>
<% @title = 'Edit College Course' %>

<div class="row">
  <div class="col-sm-3">
    <%= render 'sidenav' %>
  </div>

  <div class="col-sm-9">
    <%= render 'form', staff_course: @staff_course, path: staff_course_path(@staff_course) %>
  </div>
</div>

