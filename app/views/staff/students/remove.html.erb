<%
  @title = 'Remove Student'
  breadcrumb :staff_students_remove, @staff_student
%>

<div class="alert alert-warning">
  <strong>Warning:</strong>
  this will delete the student, their applications, payments, course selections, and all other information.
</div>

<p>Are you sure you want to remove the following student?</p>

<p><strong><%= @staff_student.username %></strong></p>

<p>For security purposes you must type the username of the student exactly as it appears in order for it to be deleted.</p>

<%= form_tag staff_student_path(@staff_student), method: :delete do %>

  <%= form_errors @staff_student %>

  <div class="form-group">
    <%= text_field_tag :username, nil, class: 'form-control', autocomplete: :off %>
  </div>

  <div class="text-right">
    <%= button_tag(:class => "btn btn-danger") do |s| %>
      <%= icon('exclamation-triangle') %>
      Remove Student
    <% end %>
  </div>
<% end %>